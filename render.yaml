services:
  - type: web
    name: unit-converter-mcp-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn converter_streamable_http_server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
